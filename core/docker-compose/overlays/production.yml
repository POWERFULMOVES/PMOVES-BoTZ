version: "3.9"

services:
  # In production, we rely on the internal network or Tailscale for access.
  # Only expose ports if strictly necessary for external load balancers.
  mcp-gateway:
    restart: always

  docling-mcp:
    restart: always

  e2b-runner:
    restart: always

  vl-sentinel:
    restart: always

  cipher-memory:
    restart: always