version: "3.9"

services:
  mcp-gateway:
    ports:
      - "2091:2091"

  docling-mcp:
    ports:
      - "3020:3020"

  e2b-runner:
    ports:
      - "7071:7071"

  vl-sentinel:
    ports:
      - "7072:7072"

  cipher-memory:
    ports:
      - "${CIPHER_MEMORY_PORT:-8081}:${CIPHER_MEMORY_PORT:-8081}"
