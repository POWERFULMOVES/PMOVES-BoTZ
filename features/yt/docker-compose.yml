version: "3.9"

services:
  yt-mini:
    build:
      context: .
      dockerfile: Dockerfile
    image: pmoves/yt-mini:dev
    container_name: yt-mini
    environment:
      PMOVES_MODE: yt-mini
    restart: unless-stopped
    # Keep alive for CLI usage
    command: tail -f /dev/null
