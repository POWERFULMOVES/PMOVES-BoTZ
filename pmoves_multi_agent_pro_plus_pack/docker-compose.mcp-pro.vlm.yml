version: "3.9"
services:
  docling-mcp:
    command: >
      sh -lc "
      pip install --no-cache-dir docling-mcp &&
      docling-mcp-server --transport streamable-http --host 0.0.0.0 --port 3020 --pipeline vlm
      "