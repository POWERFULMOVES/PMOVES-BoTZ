{
  "servers": {
    "postman-local": {
      "type": "process",
      "command": "npx",
      "args": [
        "@postman/postman-mcp-server@latest",
        "--full"
      ],
      "env": {
        "POSTMAN_API_KEY": "${POSTMAN_API_KEY}"
      }
    }
  }
}